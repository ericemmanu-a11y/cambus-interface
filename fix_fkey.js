const {Pool} = require('pg'); const pool = new Pool({user: 'cambus_admin', host: '127.0.0.1', database: 'cambus_db', password: 'cambus_admin_123', port: 5432}); async function run() { try { await pool.query('ALTER TABLE registros_vehiculos DROP CONSTRAINT IF EXISTS registros_vehiculos_id_anden_fkey'); console.log('Dropped fk anden'); } catch(e) { console.error(e); } finally { pool.end(); } } run();
